<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shoppinglist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <!--android:theme="@style/Theme.ShoppingList">-->

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ShoppingList.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".app_error_handling.FatalAppErrorActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar" />

        <!-- BEGIN STOCK -->
        <activity
            android:name=".stocks.StockActivity"
            android:label="@string/title_activity_stock_list"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <!-- END STOCK -->

        <!-- BEGIN SHOPPING LIST -->
        <activity
            android:name=".shoppinglists.ShoppingListActivity"
            android:label="@string/shopping_list"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- TODO: label -->
        <activity
            android:name=".shoppinglists.GoShoppingActivity"
            android:label="@string/shopping_list"
            android:parentActivityName=".shoppinglists.ShoppingListActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".shoppinglists.ShoppingListActivity" />
        </activity>

        <!-- TODO: label -->
        <activity
            android:name=".shoppinglists.ShoppingSummaryActivity"
            android:label="@string/shopping_list"
            android:parentActivityName=".shoppinglists.GoShoppingActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".shoppinglists.GoShoppingActivity" />
        </activity>
        <!-- END SHOPPING LIST -->
    </application>

</manifest>